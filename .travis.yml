sudo: required
services:
  - docker

before_install:
  - docker build -t doliveros/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true doliveros/docker-react npm run test -- --coverage
